<!DOCTYPE html>
  <html class="h-full">
  <head>
    <title>Walker Admin</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= yield(:ogp) %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full">
    <div class="min-h-full">
      <nav class="bg-white border-b border-gray-200">
        <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
          <div class="flex justify-between h-16">
            <div class="flex">
              <div class="flex items-center flex-shrink-0">
                <img class="w-auto h-8" src="https://tailwindui.com/img/logos/mark.svg?color=indigo&shade=600">
              </div>
              <div class="sm:-my-px sm:ml-6 sm:flex sm:space-x-8">
                <a href="<%= admin_feeds_path %>" class="inline-flex items-center px-1 pt-1 text-sm font-medium <%= request.path == admin_feeds_path ? 'text-gray-900 border-b-2 border-indigo-500' : 'text-gray-500 border-b-2 border-transparent hover:border-gray-300 hover:text-gray-700' %>" aria-current="page">Feeds</a>
                <a href="<%= admin_feed_groups_path %>" class="inline-flex items-center px-1 pt-1 text-sm font-medium <%= request.path == admin_feed_groups_path ? 'text-gray-900 border-b-2 border-indigo-500' : 'text-gray-500 border-b-2 border-transparent hover:border-gray-300 hover:text-gray-700' %>" aria-current="page">Feed Groups</a>
              </div>
            </div>
          </div>
        </div>
      </nav>
      <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
        <% if content_for?(:content) %>
          <%= yield(:content) %>
        <% else %>
          <div class="my-5">
            <%= yield(:tab_menu) %>
          </div>

          <div class="container px-5 mx-auto my-5">
            <%= yield(:breadcrumb) %>
          </div>

          <main class="container mx-auto">
            <%= yield %>
          </main>
        <% end %>
      </div>
    </div>
  </body>
</html>
